export const ERROR_MESSAGE = 'cl.setClasses error - the "classes" parameter must be an Object: { [className: string]: string } or an array of these.';

export enum Mode {
  merge = 'merge',
  replace = 'replace',
};
